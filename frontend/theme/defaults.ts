/**
 * Default internal colors for the engine and UI state.
 * These are raw values used as initializers for settings.
 * They are defined here to satisfy style governance (raw colors allowed in theme/*).
 */

export const DEFAULT_CANVAS_BG = '#212830';
export const DEFAULT_GRID_COLOR = '#313943';

// Axes
export const DEFAULT_AXIS_X_COLOR = 'rgba(239, 68, 68, 0.4)'; // Red/400 with opacity
export const DEFAULT_AXIS_Y_COLOR = 'rgba(34, 197, 94, 0.4)'; // Green/500 with opacity
export const DEFAULT_CENTER_ICON_COLOR = 'rgba(100, 116, 139, 0.5)'; // Slate/500 with opacity

// Tools
export const DEFAULT_STROKE_COLOR = '#FFFFFF';
export const DEFAULT_FILL_COLOR = '#D9D9D9';
export const DEFAULT_TEXT_SELECTION_COLOR = 'rgba(59, 130, 246, 0.3)';

