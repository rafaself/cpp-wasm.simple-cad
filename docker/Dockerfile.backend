# Dev container for the backend (FastAPI + Python 3.11)
FROM python:3.11-slim

ENV PYTHONUNBUFFERED=1
WORKDIR /workspace/backend

# Preinstall uvicorn for quicker startup; app deps are installed on container start.
RUN pip install --no-cache-dir uvicorn

EXPOSE 8000
