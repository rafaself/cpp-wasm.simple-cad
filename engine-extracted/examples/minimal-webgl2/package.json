{
  "name": "@engine-extracted/minimal-webgl2-example",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:wasm": "node ./scripts/sync-wasm.mjs",
    "dev": "npm run sync:wasm && vite",
    "build": "npm run sync:wasm && vite build",
    "preview": "vite preview --host",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.14"
  }
}
